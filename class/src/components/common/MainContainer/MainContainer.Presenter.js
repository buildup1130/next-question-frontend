import { MainContainer } from "./MainContainer.Styles";

export default function MainContainerUI({children}){
    //JS Part

    //HTML Part
    return(
        <MainContainer>
            {children}
        </MainContainer>
    )
}